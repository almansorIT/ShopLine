# ShopLine


#We are uisng PascalCase naming way for this app.

### In the Catalog.service:
we are using Vertical Slice Architecture, so don't expect to find the Models folder inside the .Domain, because the API it self is the whole microservice.
